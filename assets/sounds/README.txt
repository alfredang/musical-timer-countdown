Place the following sound files in this directory:

- click.mp3 (Button click)
- start.mp3 (Timer start)
- alarm.mp3 (Time up)

If these files are missing, the application will use a fallback generated beep sound.
